<%= form_with(model: company, local: true) do |form| %>
  <% if company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(company.errors.count, "error") %> prohibited this company from being saved:</h2>

      <ul>
      <% company.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :company_name %>
  </div>

  <div class="field">
    <%= form.label :telephone %>
    <%= form.text_field :telephone, id: :company_telephone %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_area :address, id: :company_address %>
  </div>

  <div class="field">
    <%= form.label :registration_number %>
    <%= form.text_field :registration_number, id: :company_registration_number %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.text_field :status, id: :company_status %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
